
# Имя исполняемого файла
TARGET = sensor

all: $(TARGET)

#объектники
OBJECTS = sensor.o temp_api.o

$(TARGET): $(OBJECTS)
	gcc -o $(TARGET) $(OBJECTS)

sensor.o: sensor.c temp_api.h
#	gcc -c -o sensor.o sensor.c
	gcc -c sensor.c

temp_api.o: temp_api.c temp_api.h
	gcc -c temp_api.c

clean:
	del  *.o sensor.exe
